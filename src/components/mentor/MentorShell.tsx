/**
 * MentorShell — thin re-export of AppShell, kept for backwards compatibility
 * with the existing mentor pages. New code should import AppShell directly.
 */
export { AppShell as MentorShell } from "@/components/AppShell";
